{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Debug current day",
      "program": "${workspaceFolder}/node_modules/.bin/ts-node",
      "args": [
        "${file}"
      ]
    }
  ]
}
